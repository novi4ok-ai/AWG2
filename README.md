# AWG2 — AmneziaWG Client for Ubuntu 24.04 Server

> Установка и настройка клиента AmneziaWG (AWG2) через официальный PPA-репозиторий Amnezia (`ppa:amnezia/ppa`).  
> Метод использует `amneziawg-dkms` v1.0.0 — модуль ядра (kernel-space), что обеспечивает минимальные задержки и высокую производительность по сравнению с userspace-решениями.

---

## 📋 Содержание

- [Ручная установка](#ручная-установка)
  - [Шаг 1. Подготовка системы](#шаг-1-подготовка-системы-и-установка-зависимостей)
  - [Шаг 2. Установка AmneziaWG](#шаг-2-установка-amneziawg)
  - [Шаг 3. Подготовка конфигурации](#шаг-3-подготовка-конфигурации)
  - [Шаг 4. Ручной запуск и проверка](#шаг-4-ручной-запуск-и-проверка)
  - [Шаг 5. Автозагрузка через systemd](#шаг-5-настройка-автозагрузки-systemd)
- [Автоматическая установка (скрипт)](#автоматическая-установка-скрипт)
  - [Функционал](#основной-функционал)
  - [Использование](#использование)

---

## Ручная установка

### Шаг 1. Подготовка системы и установка зависимостей

Перед установкой модуля ядра необходимо обновить списки пакетов и установить `linux-headers` для текущей версии ядра. Это обязательное условие для корректной работы системы сборки DKMS.

```bash
# Обновляем списки пакетов
sudo apt update

# Устанавливаем базовые утилиты и заголовочные файлы ядра
sudo apt install -y software-properties-common linux-headers-$(uname -r) build-essential
```

---

### Шаг 2. Установка AmneziaWG

Добавляем официальный репозиторий и устанавливаем пакеты. Мета-пакет `amneziawg` автоматически установит консольные утилиты `amneziawg-tools` и соберёт модуль ядра `amneziawg-dkms`.

```bash
# Добавляем официальный PPA-репозиторий
sudo add-apt-repository ppa:amnezia/ppa -y

# Обновляем кэш пакетов с учётом нового репозитория
sudo apt update

# Устанавливаем клиент
sudo apt install -y amneziawg
```

> **Примечание:** Предупреждения вида `Deprecated feature: REMAKE_INITRD` в процессе установки можно игнорировать — это штатное поведение DKMS в Ubuntu 24.04.

---

### Шаг 3. Подготовка конфигурации

Утилита `awg-quick` ищет конфигурации по специфичному пути: `/etc/amnezia/amneziawg/`.

```bash
# Создаём правильную структуру директорий
sudo mkdir -p /etc/amnezia/amneziawg

# Создаём файл конфигурации интерфейса awg0
sudo nano /etc/amnezia/amneziawg/awg0.conf
```

Вставьте в этот файл конфигурацию, экспортированную из приложения AmneziaVPN.

#### ⚠️ Важные проверки конфигурации (troubleshooting)

**1. Мусорные параметры**

Если в конфиге присутствуют строки вроде `I2=` или другие нестандартные параметры — **удалите их**. Парсер `awg` выдаст ошибку `Line unrecognized` при обнаружении неизвестного ключа.

Допустимые ключи:

| Секция | Параметры |
|---|---|
| `[Interface]` | `Address`, `PrivateKey`, `DNS` |
| `[Peer]` | `PublicKey`, `Endpoint`, `AllowedIPs` |
| Обфускация (DPI) | `Jc`, `Jmin`, `Jmax`, `S1`, `S2`, `H1`, `H2`, `H3`, `H4` |

**2. Настройка DNS**

Убедитесь, что параметр `DNS` в секции `[Interface]` указывает на публичные серверы — или закомментируйте его, чтобы использовался системный резолвер:

```ini
# Вариант 1: публичные DNS
DNS = 1.1.1.1, 8.8.8.8

# Вариант 2: системный резолвер
# DNS = ...
```

```bash
# Ограничиваем права доступа к файлу (обязательно — защищает PrivateKey)
sudo chmod 600 /etc/amnezia/amneziawg/awg0.conf
```

---

### Шаг 4. Ручной запуск и проверка

```bash
# Поднимаем VPN-интерфейс
sudo awg-quick up awg0

# Проверяем статус туннеля (ищем строки 'latest handshake' и 'transfer')
sudo awg show

# Проверяем, изменился ли внешний IP-адрес сервера
curl ifconfig.me
```

Если внешний IP изменился на IP вашего VPN-сервера и ошибок нет — интерфейс работает корректно.

```bash
# Опускаем интерфейс перед добавлением в автозагрузку
sudo awg-quick down awg0
```

---

### Шаг 5. Настройка автозагрузки (systemd)

Для автоматического старта VPN при загрузке используем встроенный systemd-юнит. Имя службы формируется из названия конфигурационного файла (`awg0`).

```bash
# Добавляем службу в автозагрузку
sudo systemctl enable awg-quick@awg0

# Запускаем службу
sudo systemctl start awg-quick@awg0

# Проверяем статус (должно быть 'active (running)')
sudo systemctl status awg-quick@awg0
```

---

## Автоматическая установка (скрипт)

Скрипт `install_awg.sh` автоматизирует весь процесс установки и настройки клиента AmneziaWG (kernel-space реализация) на серверах под управлением Ubuntu.

### Основной функционал

| Функция | Описание |
|---|---|
| 🔍 **Проверка окружения** | Валидирует запуск с правами `root` и принадлежность ОС к семейству Ubuntu |
| 📦 **Установка зависимостей** | Подключает `ppa:amnezia/ppa`, скачивает `linux-headers`, собирает модуль ядра через DKMS |
| ⚙️ **Интерактивная конфигурация** | Запрашивает содержимое `.conf` файла через STDIN и автоматически вычищает невалидные параметры (например, `I2=`) |
| 🔄 **Автозагрузка** | Создаёт systemd-службу для автоматического поднятия туннеля `awg0` при старте сервера |
| 🧹 **Очистка (`--clean`)** | Отключает интерфейс и полностью удаляет правила маршрутизации (iptables/nftables) и конфигурационные файлы |
| 🇷🇺 **Локализация** | Выводит статусы выполнения операций на русском языке |

---

### Использование

**Выдача прав на исполнение:**

```bash
chmod +x install_awg.sh
```

**Запуск установки:**

```bash
sudo ./install_awg.sh
```

> В процессе скрипт попросит вставить текст конфига и нажать `Ctrl+D`.

**Вызов справки:**

```bash
sudo ./install_awg.sh -h
```

**Остановка туннеля и полное удаление правил и конфигов:**

```bash
sudo ./install_awg.sh -c
```

---

<sub>Протестировано на Ubuntu 24.04 LTS Server. Используется AmneziaWG DKMS v1.0.0.</sub>
